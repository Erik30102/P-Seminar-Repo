{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Main Sandbox Application",
            "request": "launch",
            "mainClass": "com.Sandbox.SandboxApplication",
            "projectName": "Sandbox",
            "cwd": "${workspaceFolder}/Sandbox" // wichtig sonst ist greadle:run und vs code run unteschiedlich
        }
    ]
}